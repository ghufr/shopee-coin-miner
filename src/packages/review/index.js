const videoBaseUrl = "https://videoapi-vod.shopee.co.id/api/vod/preupload";
const { baseUrl } = require("../../config");

const uploadImage = () => {
  const url = `${baseUrl.rating}/image_upload`;
};
